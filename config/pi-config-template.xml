<?xml version="1.0" encoding="UTF-8"?>
<!--
  Syncthing config.xml template for relay-node-01 (Raspberry Pi relay)
  Syncthing v2.1.1+ required (candidate apt channel)

  Placeholders — replace before use:
    YOUR_DEVICE_ID          — this Pi's Syncthing Device ID
                              (find it: syncthing --device-id  or  Syncthing GUI → Actions → Show ID)
    YOUR_API_KEY            — generate a random string (openssl rand -hex 32)
    LAPTOP_1_DEVICE_ID      — Syncthing Device ID of node-us-east
    LAPTOP_2_DEVICE_ID      — Syncthing Device ID of node-us-west
    YOUR_FOLDER_ID          — shared folder ID (set the same value on all nodes)
    /home/<your-username>/sync  — path to sync folder on this Pi
    <your-username>         — Linux username running Syncthing

  Key settings that differ from Syncthing defaults:
    - connectionLimitMax = 0      (default: 1 — blocks second device from connecting)
    - connectionLimitEnough = 0   (default: 1 — same problem)
    - GUI bound to 127.0.0.1      (never expose to LAN or WAN)
    - Folder type: receiveencrypted
    - globalAnnounceEnabled: false (not needed on Tailscale mesh)
    - localAnnounceEnabled: false  (not needed on Tailscale mesh)
    - relaysEnabled: false         (Tailscale is the relay layer)
-->
<configuration version="37">

    <!-- ── This node's identity ────────────────────────────────────────── -->
    <device id="YOUR_DEVICE_ID" name="relay-node-01" compression="metadata" introducer="false" skipIntroductionRemovals="false" introducedBy="">
        <address>dynamic</address>
        <paused>false</paused>
        <autoAcceptFolders>false</autoAcceptFolders>
        <maxSendKbps>0</maxSendKbps>
        <maxRecvKbps>0</maxRecvKbps>
        <maxRequestKiB>0</maxRequestKiB>
        <untrusted>false</untrusted>
        <remoteGUIPort>0</remoteGUIPort>
    </device>

    <!-- ── Laptop node: node-us-east ───────────────────────────────────── -->
    <!--
        Address must be pinned to Tailscale IP only.
        Replace 100.x.x.1 with the actual Tailscale IP from: tailscale status
    -->
    <device id="LAPTOP_1_DEVICE_ID" name="node-us-east" compression="metadata" introducer="false" skipIntroductionRemovals="false" introducedBy="">
        <address>quic://100.x.x.1:22000</address>
        <address>tcp://100.x.x.1:22000</address>
        <paused>false</paused>
        <autoAcceptFolders>false</autoAcceptFolders>
        <maxSendKbps>0</maxSendKbps>
        <maxRecvKbps>0</maxRecvKbps>
        <maxRequestKiB>0</maxRequestKiB>
        <untrusted>false</untrusted>
        <remoteGUIPort>0</remoteGUIPort>
    </device>

    <!-- ── Laptop node: node-us-west ───────────────────────────────────── -->
    <!--
        Replace 100.x.x.2 with the actual Tailscale IP from: tailscale status
    -->
    <device id="LAPTOP_2_DEVICE_ID" name="node-us-west" compression="metadata" introducer="false" skipIntroductionRemovals="false" introducedBy="">
        <address>quic://100.x.x.2:22000</address>
        <address>tcp://100.x.x.2:22000</address>
        <paused>false</paused>
        <autoAcceptFolders>false</autoAcceptFolders>
        <maxSendKbps>0</maxSendKbps>
        <maxRecvKbps>0</maxRecvKbps>
        <maxRequestKiB>0</maxRequestKiB>
        <untrusted>false</untrusted>
        <remoteGUIPort>0</remoteGUIPort>
    </device>

    <!-- ── Shared folder ───────────────────────────────────────────────── -->
    <!--
        type="receiveencrypted":
          - This node stores AES-SIV encrypted blobs only
          - No folder password is set here — the relay cannot decrypt files
          - The password is set on the laptop nodes only
          - Do NOT change the type to "sendreceive" unless you want plaintext on Pi

        id must match the folder ID used on all laptop nodes exactly.
    -->
    <folder id="YOUR_FOLDER_ID" label="Sync" path="/home/<your-username>/sync" type="receiveencrypted" rescanIntervalS="3600" fsWatcherEnabled="true" fsWatcherDelayS="10" ignorePerms="false" autoNormalize="true">
        <filesystemType>basic</filesystemType>
        <device id="LAPTOP_1_DEVICE_ID" introducedBy="" encryptionPassword="">
            <encryptionPassword></encryptionPassword>
        </device>
        <device id="LAPTOP_2_DEVICE_ID" introducedBy="" encryptionPassword="">
            <encryptionPassword></encryptionPassword>
        </device>
        <minDiskFreePct>1</minDiskFreePct>
        <versioning></versioning>
        <copiers>0</copiers>
        <pullerMaxPendingKiB>0</pullerMaxPendingKiB>
        <hashers>0</hashers>
        <order>random</order>
        <ignoreDelete>false</ignoreDelete>
        <scanProgressIntervalS>0</scanProgressIntervalS>
        <pullerPauseS>0</pullerPauseS>
        <maxConflicts>10</maxConflicts>
        <disableSparseFiles>false</disableSparseFiles>
        <disableTempIndexes>false</disableTempIndexes>
        <paused>false</paused>
        <weakHashThresholdPct>25</weakHashThresholdPct>
        <markerName>.stfolder</markerName>
        <copyOwnershipFromParent>false</copyOwnershipFromParent>
        <modTimeWindowS>0</modTimeWindowS>
        <maxConcurrentWrites>2</maxConcurrentWrites>
        <disableFsync>false</disableFsync>
        <blockPullOrder>standard</blockPullOrder>
        <copyRangeMethod>standard</copyRangeMethod>
        <caseSensitiveFS>false</caseSensitiveFS>
        <junctionsAsDirs>false</junctionsAsDirs>
        <syncOwnership>false</syncOwnership>
        <sendOwnership>false</sendOwnership>
        <syncXattrs>false</syncXattrs>
        <sendXattrs>false</sendXattrs>
        <xattrFilter></xattrFilter>
    </folder>

    <!-- ── GUI settings ────────────────────────────────────────────────── -->
    <!--
        SECURITY: GUI is bound to 127.0.0.1 only.
        Access via SSH tunnel: ssh -L 8384:127.0.0.1:8384 <user>@<tailscale-ip>
        Then open http://127.0.0.1:8384 in your local browser.
        Never set address to 0.0.0.0 on a relay node.
    -->
    <gui enabled="true" tls="false" debugging="false">
        <address>127.0.0.1:8384</address>
        <apikey>YOUR_API_KEY</apikey>
        <theme>default</theme>
    </gui>

    <!-- ── LDAP (disabled) ─────────────────────────────────────────────── -->
    <ldap></ldap>

    <!-- ── Options ─────────────────────────────────────────────────────── -->
    <!--
        KEY CHANGES from defaults:
          connectionLimitEnough: 0  (was 1 — allows only 1 device to connect simultaneously)
          connectionLimitMax: 0     (was 1 — hard cap on connections)
          globalAnnounceEnabled: false  (not needed, Tailscale handles peer discovery)
          localAnnounceEnabled: false   (not needed on a headless relay)
          relaysEnabled: false          (Tailscale is the relay layer; Syncthing relays not needed)
    -->
    <options>
        <listenAddress>default</listenAddress>
        <globalAnnounceServer>default</globalAnnounceServer>
        <globalAnnounceEnabled>false</globalAnnounceEnabled>
        <localAnnounceEnabled>false</localAnnounceEnabled>
        <localAnnouncePort>21027</localAnnouncePort>
        <localAnnounceMCAddr>[ff12::8384]:21027</localAnnounceMCAddr>
        <maxSendKbps>0</maxSendKbps>
        <maxRecvKbps>0</maxRecvKbps>
        <reconnectionIntervalS>60</reconnectionIntervalS>
        <relaysEnabled>false</relaysEnabled>
        <relayReconnectIntervalM>10</relayReconnectIntervalM>
        <startBrowser>false</startBrowser>
        <natEnabled>false</natEnabled>
        <natLeaseMinutes>60</natLeaseMinutes>
        <natRenewalMinutes>30</natRenewalMinutes>
        <natTimeoutSeconds>10</natTimeoutSeconds>
        <urAccepted>-1</urAccepted>
        <urSeen>3</urSeen>
        <urUniqueID></urUniqueID>
        <urURL>https://data.syncthing.net/newdata</urURL>
        <urPostInsecurely>false</urPostInsecurely>
        <urInitialDelayS>1800</urInitialDelayS>
        <autoUpgradeIntervalH>0</autoUpgradeIntervalH>
        <upgradeToPreReleases>false</upgradeToPreReleases>
        <keepTemporariesH>24</keepTemporariesH>
        <cacheIgnoredFiles>false</cacheIgnoredFiles>
        <progressUpdateIntervalS>5</progressUpdateIntervalS>
        <limitBandwidthInLan>false</limitBandwidthInLan>
        <minHomeDiskFree unit="%">1</minHomeDiskFree>
        <releasesURL>https://upgrades.syncthing.net/meta.json</releasesURL>
        <overwriteRemoteDeviceNamesOnConnect>false</overwriteRemoteDeviceNamesOnConnect>
        <tempIndexMinBlocks>10</tempIndexMinBlocks>
        <unackedNotificationID></unackedNotificationID>
        <trafficClass>0</trafficClass>
        <setLowPriority>true</setLowPriority>
        <maxFolderConcurrency>0</maxFolderConcurrency>
        <crURL>https://crash.syncthing.net/newcrash</crURL>
        <crashReportingEnabled>false</crashReportingEnabled>
        <stunKeepaliveStartS>180</stunKeepaliveStartS>
        <stunKeepaliveMinS>20</stunKeepaliveMinS>
        <stunServer>default</stunServer>
        <databaseTuning>auto</databaseTuning>
        <maxConcurrentIncomingRequestKiB>0</maxConcurrentIncomingRequestKiB>
        <announceLANAddresses>false</announceLANAddresses>
        <sendFullIndexOnUpgrade>false</sendFullIndexOnUpgrade>
        <featureFlag></featureFlag>
        <connectionLimitEnough>0</connectionLimitEnough>
        <connectionLimitMax>0</connectionLimitMax>
        <insecureAllowOldTLSVersions>false</insecureAllowOldTLSVersions>
        <connectionPriorityTCPLAN>10</connectionPriorityTCPLAN>
        <connectionPriorityQUICLAN>20</connectionPriorityQUICLAN>
        <connectionPriorityTCPWAN>30</connectionPriorityTCPWAN>
        <connectionPriorityQUICWAN>40</connectionPriorityQUICWAN>
        <connectionPriorityRelay>50</connectionPriorityRelay>
        <connectionPriorityUpgradeThreshold>0</connectionPriorityUpgradeThreshold>
    </options>

    <!-- ── Defaults (leave as-is) ──────────────────────────────────────── -->
    <defaults>
        <folder id="" label="" path="~" type="sendreceive" rescanIntervalS="3600" fsWatcherEnabled="true" fsWatcherDelayS="10" ignorePerms="false" autoNormalize="true">
            <filesystemType>basic</filesystemType>
            <minDiskFreePct>1</minDiskFreePct>
            <versioning></versioning>
            <order>random</order>
            <ignoreDelete>false</ignoreDelete>
            <maxConflicts>10</maxConflicts>
            <markerName>.stfolder</markerName>
        </folder>
        <device compression="metadata" introducer="false" skipIntroductionRemovals="false" introducedBy="">
            <address>dynamic</address>
            <autoAcceptFolders>false</autoAcceptFolders>
        </device>
    </defaults>

</configuration>
